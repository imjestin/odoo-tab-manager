{
  "name": "odoo-tab-manager",
  "version": "2.0.0",
  "description": "A powerful tab manager extension for Odoo with personalized new tab, mode switching, and Odoo detection features",
  "scripts": {
    "build:ts": "tsc --project tsconfig.popup.json && tsc --project tsconfig.app.json && tsc --project tsconfig.content.json && tsc --project tsconfig.mode-sync.json",
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/output.css",
    "build:copy": "cp theme.js dist/",
    "build": "npm run build:ts && npm run build:css && npm run build:copy",
    "watch:ts": "concurrently \"tsc --project tsconfig.popup.json --watch\" \"tsc --project tsconfig.app.json --watch\" \"tsc --project tsconfig.content.json --watch\"",
    "watch:css": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "watch": "concurrently \"npm run watch:ts\" \"npm run watch:css\"",
    "dev": "concurrently \"npm run watch:ts\" \"npm run watch:css\""
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "concurrently": "^8.0.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.0.0"
  }
}
